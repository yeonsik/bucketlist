{
  "name": "bucketListApp",
  "version": "0.1.0",
  "description": "A REST server for bucketListApp",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yeonsik",
  "license": "MIT",
  "engines": {
    "node": "0.10.x"
  },
  "dependencies": {
    "morgan": "^1.5.1",
    "bcrypt": "^0.8.0",
    "restify": "^2.8.4",
    "mongojs": "^0.18.0"
  }
}
